name: Build macOS Release

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install
        working-directory: scenarist

      - name: Fix electron-builder permissions
        run: chmod +x ./node_modules/.bin/electron-builder
        working-directory: scenarist

      - name: Fix app-builder permissions
        run: chmod +x ./node_modules/app-builder-bin/mac/*
        working-directory: scenarist

      - name: Build Electron app for macOS
        run: npm run build
        working-directory: scenarist

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: scenarist-mac-dmg
          path: scenarist/dist/*.dmg

      - name: Upload ZIP artifact
        uses: actions/upload-artifact@v4
        with:
          name: scenarist-mac-zip
          path: scenarist/dist/*.zip